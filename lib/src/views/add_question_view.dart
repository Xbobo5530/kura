import 'package:flutter/material.dart';

class AddQuestionView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('AddQuestionView'),
    );
  }
}